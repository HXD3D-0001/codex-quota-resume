# Codex 限额恢复助手

项目：codex-quota-resume。目标：修复旧悬浮条仅读取会话日志导致重置后显示陈旧的问题，并在真实额度恢复后继续因限额失败的原任务。

已确认范围：用户授权独立 GitHub 仓库；自动恢复因限额中断的任务，其他任务只有明确标记待继续后才可恢复。每五轮对话同步项目，提交排除本地状态与凭据。

## 实现约定

- Windows、Python 3.11+、Node.js 18+；监测核心为标准库，UI 使用独立目录中的 PySide6 Essentials。360×38 逻辑像素，原生 Acrylic、高 DPI 清晰文字、完全鼠标穿透，控制放到系统托盘。
- 本地守护程序独立于模型运行；通过已安装 Codex app tools 的本地管道协议读取实时额度、任务状态、发送后续消息。此桥接接口来自本机捆绑插件，属于版本相关接口；失效时停止续跑并显示连接错误，不退回盲目 CLI resume。
- 常规 30 秒查询，距重置 30 秒内最多 5 秒间隔，准确调度到重置时间；重置后额度仍阻塞时每 5 秒重查。只有新鲜查询证实所有适用额度可用才续跑。周额度仍耗尽、数据缺失、断网均不放行。
- 最新一轮必须明确失败且错误为 usage limit；不能仅凭 idle、未归档或文本提到额度判定。正在工作、等待输入、手动中断、已完成任务不自动恢复。
- 手动待继续标记绑定最新轮次；后来有新轮次则标记失效。每次发送前重查任务和额度，串行发送。
- SQLite 持久化每个任务/失败轮次的发送记录。发送前先记录 dispatching；请求结果未知时不自动重发，提示人工检查。成功记录 sent。程序单实例。
- 新安装时只监测安装后的限额失败；之前的失败通过手动标记显式纳入，避免重启已被替代的旧项目。
- 保留原任务模型、思考强度和权限设置；只发继续原任务的提示，不传覆盖参数。
- 额度缓存仅供展示；过期明显标记 stale，不把时间经过当作额度恢复。不保存账户 ID、凭据或对话正文。
- Codex 必须运行、电脑必须清醒；恢复运行后立即重新核查。
- 生命周期监听器每 2 秒检查当前 Windows 会话的 OpenAI.Codex 应用包主进程，排除 CLI 与普通 ChatGPT。Codex 启动才创建监测/UI 子进程，退出后发送退出信号；8 秒未退出只结束自己启动的子进程树。监听器无额度查询。监听器与监测各自单实例，托盘手动退出保持到下一次 Codex 启动。
- 启动同步不只依赖登录快捷方式：登录计划任务的动作是看门狗（`watchdog.py`），看门狗每 2 秒确认监听器存活且心跳新鲜，进程被杀后约 1 秒重新拉起；监听器自身另有崩溃重启（指数退避，连续快速崩溃达上限后记录并退出）；计划任务是第三层，用于看门狗本身被杀的情况（约 1 分钟）。实测：任务动作被外部强杀时任务停在 Ready，`RestartCount` 不触发，因此外部结束只能由看门狗兜底。
- 监听器每 2 秒写心跳；心跳中断时 UI 状态点转黄说明原因。`scripts/start.ps1` 与任务注册都会清除上一次遗留的停止请求，显式启动优先于旧停止请求（否则新启动会立刻退出）。`python control.py doctor` 交叉校验状态目录重定向、UI 运行时可达性、任务注册、看门狗与监听器 PID、心跳新鲜度与额度报告，失败时退出码为 1；监听器随 Codex 退出属正常，doctor 必须与崩溃区分开。

## 参考

- https://learn.chatgpt.com/docs/app-server （实时额度与原任务接口）
- https://github.com/roboticsdao/codex-usage-monitor （本地 app-server 额度查询）
- https://github.com/omi-last-stand/codex-usage-monitor/blob/main/docs/event-commands.md （真实重置后触发）
- https://github.com/Justin1491/codex-dashboard （自动续跑，但 --last 不适用多任务精确恢复）

不复制这些项目代码；只参考机制。UI 重写与错误状态漏检修复见 incident-2026-09-22.md。
